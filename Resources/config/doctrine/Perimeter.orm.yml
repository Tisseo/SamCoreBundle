CanalTP\SamCoreBundle\Entity\Perimeter:
    type: entity
    table: public.t_perimeter_per
    repositoryClass: CanalTP\SamCoreBundle\Entity\PerimeterRepository
    id:
        id:
            type: integer
            id: true
            column: per_id
            generator:
                strategy: AUTO
    fields:
        externalCoverageId:
            type: string
            length: 255
            column: per_external_coverage_id
        externalNetworkId:
            type: string
            length: 255
            column: per_external_network_id
    uniqueConstraints:
        client_id_external_coverage_id_external_network_id_idx:
            columns: [ cli_id, per_external_coverage_id, per_external_network_id ]
    manyToOne:
        client:
            targetEntity: CanalTP\SamCoreBundle\Entity\Client
            inversedBy: perimeters
            joinColumn:
                name: cli_id
                referencedColumnName: cli_id
    lifecycleCallbacks: {  }
