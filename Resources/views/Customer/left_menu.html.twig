{% if (is_granted('BUSINESS_CREATE_CLIENT')) %}
    <a class="btn btn-primary" href="{{ path('sam_customer_new') }}">
        <span class="glyphicon glyphicon-plus"></span>
        {{'customer.new.title'|trans}}
    </a>
{% endif %}
